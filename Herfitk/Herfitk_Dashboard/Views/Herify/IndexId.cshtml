@model Herfitk.Core.Models.Data.Herfiy

@{
    ViewData["Title"] = "IndexId";
}

<h1 class="text-center">Details</h1>
<div class="container">
    <h1 class="text-center">Details of Herify</h1>
    <hr />
    <div>
        <dl class="row">
            <dt class="col-sm-2">Category Name</dt>
            <dd class="col-sm-10">@Html.DisplayFor(model => model.Id)</dd>
            <dt class="col-sm-2">Name</dt>
            <dd class="col-sm-10">@Html.DisplayFor(model => model.Zone)</dd>
            <dt class="col-sm-2">Phone</dt>
            <dd class="col-sm-10">@Html.DisplayFor(model => model.History)</dd>
            <dt class="col-sm-2">Speciality</dt>
            <dd class="col-sm-10">@Html.DisplayFor(model => model.Speciality)</dd>
@*             <dt class="col-sm-2">Image</dt>
            <dd class="col-sm-10"> *@
          @*   <dd class="col-sm-10">
                    @if (Model.PersonalImage != null && Model.PersonalImage.Length > 0)
                    {
                    <img src="@Model.PersonalImage" alt="Image" style="max-width: 100px; max-height: 100px;" />
                    }
            </dd> *@
        </dl>
    </div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.Id">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
